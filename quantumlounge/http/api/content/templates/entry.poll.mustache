<div class="ql-poll" id="ql-poll-{{ _id }}">
    <div class="ql-poll-body">
        <h4>{{ content }}</h4>
        <form id="ql-poll-form-{{_id}}" class="ql-poll-form">
            {{#answers}}
                <div class="ql-poll-answer">
                    <input  class="ql-poll-box" 
                            id = "ql-poll-box-{{_id}}"
                            data-qid="{{_id}}" 
                            data-aid="{{no}}"
                            type="radio" 
                            name="answer"
                            value="{{no}}" />
                    <label>{{ title }}</label>
                </div>
            {{/answers}}
        </form>
    </div>
</div>
